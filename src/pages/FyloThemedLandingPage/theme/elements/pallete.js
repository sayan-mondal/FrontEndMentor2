const PRIMARY = {
	1: 'hsl(217, 28%, 15%)', //Dark Blue (intro and email sign up background)
	2: 'hsl(218, 28%, 13%)', //Dark Blue (main background)
	3: 'hsl(216, 53%, 9%)', //Dark Blue (footer background)
	4: 'hsl(219, 30%, 18%)', //Dark Blue (testimonials background)
};

const ACCENT = {
	1: 'hsl(176, 68%, 64%)', //Cyan (inside call-to-action gradient)
	2: 'hsl(198, 60%, 50%)', //Blue (inside call-to-action gradient)
	3: 'hsl(0, 100%, 63%)', //Light Red (error)
};

const NEUTRAL = {
	1: 'hsl(0, 0%, 100%)', //White
};

const GRADIENTS = {};

const PALLETE = {
	primary: PRIMARY,
	accent: ACCENT,
	neutral: NEUTRAL,
	gradients: GRADIENTS,
};

export default PALLETE;
